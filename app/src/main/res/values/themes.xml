<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CountDown" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimaryDark">@color/light_black</item>
        <item name="colorPrimaryVariant">@color/light_black</item>
        <item name="colorOnPrimary">@color/light_black</item>
        <item name="android:navigationBarColor">@color/light_black</item>
    </style>

    <style name="Theme.CountDown" parent="Base.Theme.CountDown" />
</resources>